{{outlet}}

<div class="btn-toolbar">
  <div class="btn-group">
    {{link-to 'Create Game' 'index.create' class="btn btn-primary"}}
    {{link-to 'Join Game' 'index.join' class="btn"}}
  </div>
</div>
